[project]
name = "archie-cli"
dynamic = ["version"]

description = "Archie CLI client"
requires-python = ">=3.13,<3.14"
dependencies = [
    "archie-orchestrator",
    "archie-shared",
    "boto3>=1.35",
    "click>=8,<9",
    "httpx>=0.27",
    "python-ulid>=3.1.0",
    "textual>=1.0",
    "typing-extensions>=4.0",
    "websockets>=13",
]

[project.scripts]
archie = "archie_cli.cli:main"

[tool.hatch.version]
path = "../shared/src/archie_shared/version.py"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/archie_cli"]
